import sqlite3
from globals import *

# conn = sqlite3.connect(LOCAL_STORAGE_PATH)
# cursor = conn.cursor()
# cursor.execute(f"SELECT key, value FROM {LOCAL_STORAGE_TABLE} WHERE scope = '/{reversed(ED)}.:file';")
# rows = cursor.fetchall()